name = "ironman-observer-next"
pages_build_output_dir = "web/dist"
# Custom domains are configured in the Cloudflare dashboard (Pages project → Custom domains),
# not via wrangler routes (a Workers-only key that is a silent no-op for Pages).
